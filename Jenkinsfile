pipeline {
    agent any

    stages {
        stage('Test Stage') {
            steps {
                echo 'Pipeline is working'
            }
        }

        stage('Shell Test') {
            steps {
                sh 'echo Hello from Jenkins'
            }
        }
    }
}
