pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                echo 'Working now'
            }
        }

        stage('Shell') {
            steps {
                sh 'echo Hello from EC2'
            }
        }
    }
}
